// Cria duas variáveis chamadas "valor1" e "valor2" e atribui valores numéricos.
let valor1 = 10;
let valor2 = 20;

// Realiza a soma dos valores e armazena o resultado em uma terceira variável chamada "resultado".
let resultado = valor1 + valor2;

// Exibe a mensagem com a soma no console.
console.log(`A soma de ${valor1} e ${valor2} é igual a ${resultado}.`);
