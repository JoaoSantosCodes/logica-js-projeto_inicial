// Suponhamos que o saldo da conta seja 500.
let saldoConta = 500;

// Cria uma mensagem usando uma template string para incluir o valor do saldo.
let mensagem = `Seu saldo atual é de R$ ${saldoConta.toFixed(2)}.`;

// Exibe a mensagem em um alerta.
alert(mensagem);
