// Inicializa a variável contador com 1.
let contador = 1;

// Enquanto o contador for menor ou igual a 10, o loop continuará.
while (contador <= 10) {
    // Imprime o valor atual do contador no console.
    console.log(contador);

    // Incrementa o contador em 1 para a próxima iteração.
    contador++;
}
