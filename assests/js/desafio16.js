// Cria duas variáveis chamadas "valor1" e "valor2" e atribui valores numéricos.
let valor1 = 30;
let valor2 = 15;

// Realiza a subtração dos valores e armazena o resultado em uma terceira variável chamada "resultado".
let resultado = valor1 - valor2;

// Exibe a mensagem com a diferença no console.
console.log(`A diferença entre ${valor1} e ${valor2} é igual a ${resultado}.`);
